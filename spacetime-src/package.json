{
	"name": "tmnext-predictor-module",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"build": "spacetime build"
	},
	"dependencies": {
		"spacetimedb": "^2.1.0"
	},
	"devDependencies": {
		"typescript": "^6.0.2"
	}
}
